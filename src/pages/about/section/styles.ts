import styled from "styled-components";

export const SectionContent = styled.section`
  padding: 70px 15px;

  .about-items {
    position: relative;
    padding: 15px;

    img {
      border-radius: 10px;
    }

    h2 {
      font-size: 28px;
      font-weight: 500;

      color: var(--dark);

      margin-bottom: 20px;
      line-height: 46px;
    }

    p {
      line-height: 28px;
    }
  }
`;
